% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R, R/pclm.R
\name{print.pclm}
\alias{print.pclm}
\alias{summary.pclm}
\alias{coef.pclm}
\alias{fitted.pclm}
\alias{logLik.pclm}
\alias{plot.pclm}
\alias{predict.pclm}
\alias{quantile.pclm}
\alias{pclm}
\title{Frequentist penalised composite link model for grouped data}
\usage{
\method{print}{pclm}(x, digits = 4L, ...)

\method{summary}{pclm}(object, probs = c(0.05, 0.1, 0.25, 0.5, 0.75, 0.9, 0.95), ...)

\method{coef}{pclm}(object, ...)

\method{fitted}{pclm}(object, ...)

\method{logLik}{pclm}(object, ...)

\method{plot}{pclm}(
  x,
  add = FALSE,
  density_col = "black",
  hist_col = grDevices::adjustcolor("grey70", alpha.f = 0.6),
  hist_border = "grey40",
  xlab = "y",
  ylab = "Density",
  main = "Frequentist PCLM fit",
  xlim = NULL,
  ylim = NULL,
  lwd = 2,
  ...
)

\method{predict}{pclm}(object, newdata, ...)

\method{quantile}{pclm}(x, probs = c(0.25, 0.5, 0.75), ...)

pclm(
  m,
  wide_breaks,
  a = NULL,
  b = NULL,
  ngrid = 100L,
  ndx = 17L,
  degree = 3L,
  penalty_order = 3L,
  tau = NULL,
  select = c("BIC", "AIC"),
  max_iter = 100L,
  tol = 1e-07,
  phi_start = NULL,
  verbose = FALSE
)
}
\arguments{
\item{x}{A \code{"pclm"} object (in \code{print}, \code{plot} and
\code{quantile} methods).}

\item{digits}{Number of significant digits used in \code{print}.}

\item{...}{Further arguments. Currently ignored by all methods.}

\item{object}{A \code{"pclm"} object (in \code{summary}, \code{coef},
\code{fitted}, \code{logLik} and \code{predict} methods).}

\item{probs}{Probabilities at which to compute quantiles.}

\item{add}{Logical: if \code{TRUE}, add to an existing plot.}

\item{density_col}{Colour for the fitted density line.}

\item{hist_col}{Fill colour for the histogram rectangles.}

\item{hist_border}{Border colour for histogram rectangles.}

\item{xlab, ylab, main, ylim, xlim, lwd}{Standard graphical parameters
(passed to plotting methods of \code{pclm} / \code{bpclm} fits).}

\item{newdata}{Optional numeric vector of points at which the fitted
density is evaluated by \code{predict}. If missing, the density is
returned at the fine-grid midpoints.}

\item{m}{Numeric vector of non-negative wide-bin counts (length
\eqn{J}).}

\item{wide_breaks}{Wide-bin boundaries; either a \eqn{J + 1} vector
(contiguous bins partitioning \eqn{(a, b)}) or a \eqn{J \times 2}
matrix/data frame of (lower, upper) limits.}

\item{a, b}{Lower and upper limits of the support \eqn{(a, b)} on
which to estimate the density. Defaults to the smallest lower limit
and largest upper limit of \code{wide_breaks}.}

\item{ngrid}{Number of fine-grid intervals \eqn{I}. Defaults to 100.}

\item{ndx}{Number of equally-spaced knot intervals on \eqn{(a, b)}.
The number of B-splines is \code{ndx + degree}. Default 17 (so
\eqn{K = 20} cubic B-splines, matching the paper's examples).}

\item{degree}{B-spline degree (default 3, cubic).}

\item{penalty_order}{Order \eqn{r} of the difference penalty
(default 3, as in the paper's examples).}

\item{tau}{Either a positive scalar smoothing parameter, or a numeric
vector of candidate values to evaluate. If \code{NULL} (the
default), a logarithmically-spaced grid from \code{1e-2} to
\code{1e6} (length 25) is used.}

\item{select}{Information criterion used to pick \eqn{\tau} when a
grid is provided: either \code{"BIC"} (default) or \code{"AIC"}.}

\item{max_iter}{Maximum number of scoring iterations.}

\item{tol}{Convergence tolerance on the largest absolute change in
\eqn{\phi}.}

\item{phi_start}{Optional starting value for \eqn{\phi} (length
\code{ndx + degree}). Defaults to the zero vector (uniform
density).}

\item{verbose}{Logical: if \code{TRUE}, print one line per scoring
iteration.}
}
\value{
An object of class \code{"pclm"}, a list with components:
  \describe{
    \item{phi}{Estimated B-spline coefficients (length \eqn{K},
      summing to 0).}
    \item{tau}{Selected (or supplied) smoothing parameter.}
    \item{tau_grid}{Numeric vector: the full grid of \eqn{\tau}
      values evaluated. \code{NULL} if a single \eqn{\tau} was
      passed.}
    \item{ic}{Information-criterion value at the selected
      \eqn{\tau}.}
    \item{ic_grid}{Vector of IC values at the candidate \eqn{\tau}
      values (\code{NULL} if a single \eqn{\tau}).}
    \item{select}{Which IC was used (\code{"BIC"} or \code{"AIC"}).}
    \item{logL}{Log-likelihood at convergence.}
    \item{edf}{Effective degrees of freedom,
      \eqn{\mathrm{tr}((I + \tau P)^{-1} I)}.}
    \item{vcov}{Estimated variance-covariance matrix of \eqn{\phi}
      (Eq. 3 of the paper).}
    \item{pi}{Latent grid probabilities \eqn{\pi_i}.}
    \item{gamma}{Fitted wide-bin probabilities \eqn{\gamma_j}.}
    \item{fitted_counts}{\eqn{m_+ \gamma}, the multinomial
      expectations.}
    \item{grid}{Fine-grid breakpoints (length \eqn{I + 1}).}
    \item{grid_mid}{Fine-grid midpoints (length \eqn{I}).}
    \item{basis}{The \code{"pclm_basis"} object used.}
    \item{C}{The bin matrix \eqn{C}.}
    \item{m, wide_breaks}{The supplied data.}
    \item{call}{The matched call.}
  }
}
\description{
Fits the model of Lambert and Eilers (2009) by penalised maximum
likelihood. The log of the latent density is modelled as a linear
combination of B-splines with an \eqn{r}th-order difference penalty
on the coefficients. Wide-bin counts \code{m} are assumed to follow a
multinomial distribution with probabilities
\eqn{\gamma = C \pi(\phi)}.
}
\details{
For a given smoothing parameter \eqn{\tau}, the fit is obtained by
the penalised scoring algorithm (Eq. 2 of the paper). When a grid of
candidate \eqn{\tau} values is supplied via \code{tau} (a numeric
vector of length \eqn{> 1}), the value minimising the chosen
information criterion (\code{select = "BIC"}, the default, or
\code{"AIC"}) is selected. The variance-covariance matrix of the
coefficients is the inverse of the penalised Fisher information at
convergence (Eq. 3 of the paper).
}
\examples{
data(bloodlead)
fit <- pclm(m = bloodlead$count,
            wide_breaks = with(bloodlead, cbind(lower, upper)),
            a = 0, b = 80, ngrid = 80, ndx = 17, degree = 3,
            penalty_order = 3)
summary(fit)

}
\references{
Eilers, P. H. C. (2007). Ill-posed problems with counts, the
composite link model and penalized likelihood. \emph{Statistical
Modelling}, 7(3), 239--254. (Original frequentist penalised PCLM.)

Lambert, P. and Eilers, P. H. C. (2009). Bayesian density estimation
from grouped continuous data. \emph{Computational Statistics and
Data Analysis}, 53(4), 1388--1399. (Section 2, Eq. (2): the specific
penalised-scoring form implemented here.)
}
\seealso{
\code{\link{bpclm}} for the Bayesian variant.
}
